services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: Manas@123
      MYSQL_DATABASE: pipetier_db
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - mysql

volumes:
  mysql_data:
